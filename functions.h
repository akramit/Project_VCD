#include<bits/stdc++.h>
using namespace std;

int binary_val(string ); //gives binary val of string
vector<string> generate_Universe(int); //universe of dim n
vector<string> Intersection_F_S(string); //usual meaning
bool compare_set(vector<string> ,vector<string> ); //compate two sets whether they are same
int count_set_bits(int ); //# 1's in bin rep of a num
vector<vector<string>> generate_subset_size_d(vector<string>,int);
vector<vector<string>> getPowerSet(vector<string> );

// All functions
